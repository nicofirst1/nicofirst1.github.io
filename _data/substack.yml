# Auto-maintained by .github/workflows/substack-backlinks.yml — do not edit by hand.
# Slugs of posts confirmed live on Substack (HTTP 200 at <_config substack>/p/<slug>).
# Substack reuses the site slug, so the backlink URL is <_config substack> + /p/ + slug.
# _layouts/blogs.html renders the "Read & discuss on Substack" link for slugs listed here.
